export declare enum PERMISSION {
    READ = ".read",
    WRITE = ".write",
    CREATE = "document.create",
    UPDATE = "document.update",
    DELETE = "document.delete",
    AGGREGATE = ".aggregate"
}
